# .First <- function() {
#   source("https://raw.githubusercontent.com/taylorgrant/sandbox/master/theme_twg.R")
#   
# }
# 
# # load color palette
# my_colors <- function (palette = "cb"){
#   cb.palette <- c("#000000", "#E69F00", "#56B4E9", "#009E73", 
#                   "#F0E442", "#0072B2", "#D55E00", "#CC79A7")
#   rcb.palette <- rev(cb.palette)
#   bly.palette <- c("#E69F00", "#0072B2", "#000000", "#56B4E9", 
#                    "#009E73", "#F0E442", "#D55E00", "#CC79A7", "#BB162C")
#   solr5.palette <- c("#003f5c", "#58508d", "#bc5090", "#ff6361", "#ffa600")
#   solr8.palette <- c("#003f5c", "#2f4b7c", "#665191", "#a05195", 
#                      "#d45087", "#f95d6a", "#ff7c43", "#ffa600")
#   if (palette == "cb") 
#     return(cb.palette)
#   else if (palette == "rcb") 
#     return(rcb.palette)
#   else if (palette == "bly") 
#     return(bly.palette)
#   else if (palette == "solr5")
#     return(solr5.palette)
#   else if (palette == 'solr8')
#     return(solr8.palette)
#   else stop("Choose cb, rcb, or bly only.")
# }
# 
# source("~/R/dng_functions/show_pal.R")

options(
  blogdown.author = "analytics",
  blogdown.ext = ".Rmd",
  blogdown.subdir = "post",
  blogdown.yaml.empty = TRUE,
  blogdown.new_bundle = TRUE,
  blogdown.title_case = TRUE,
  blogdown.hugo.version = "0.80.0",
  blogdown.knit.on_save = TRUE
)
